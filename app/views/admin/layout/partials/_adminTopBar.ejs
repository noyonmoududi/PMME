<!-- Navbar -->
<nav class="main-header navbar navbar-expand navbar-white navbar-light">
    <!-- Left navbar links -->
    <ul class="navbar-nav">
      <li class="nav-item">
        <a class="nav-link" data-widget="pushmenu" id="expand_bar" href="#" role="button"><i class="fas fa-bars"></i></a>
      </li>
    </ul>

    <!-- Right navbar links -->
    <% if (Request.session.user) {  %>
      <ul class="navbar-nav ml-auto">
        <!-- Notifications Dropdown Menu -->

          <li class="nav-item dropdown">
            <a class="nav-link" data-toggle="dropdown" href="#">
                <div class="media">
                  <% if(Request.session.user.photo){ %>
                    <img src="/file_storage/user-photos/<%- Request.session.user.photo %>" alt="User" height="30px" width="30px" class="img-circle">
                    <% }else{ %>
                      <img src="/img/users/empty.png" alt="User" height="30px" width="30px" class="img-circle">
                    <% } %>
                    &nbsp; &nbsp;
                    <div class="media-body">
                        <b><%- Request.session.user.name %></b>
                    </div>
                    </div>
            </a>
            <div class="dropdown-menu dropdown-menu-lg dropdown-menu-right">
              <div class="dropdown-divider"></div>
              <a href="/admin/profile" class="dropdown-item">
                <i class="fas fa-users mr-2"></i> PROFILE
              </a>
              <div class="dropdown-divider"></div>
              <form action="/admin/logout" method="post">
                <button type="submit" class="dropdown-item"><i class="fas fa-sign-out-alt mr-2"></i> SIGN OUT</button>
              </form>
            </div>
          </li>
      </ul>
    <% }%>
  </nav>
  <!-- /.navbar -->
